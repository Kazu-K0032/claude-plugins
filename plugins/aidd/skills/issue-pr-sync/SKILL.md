---
name: issue-pr-sync
description: 指定した Issue と PR を、現在のブランチ差分とレビュー対応を踏まえて最新の内容に更新する。本文は承認後に直接反映し、ラベル等のサイドバーは適用コマンドを提案する。追加対応やレビュー対応で Issue / PR の記述が実態とずれた時に使用する
argument-hint: "<Issue番号|URL> <PR番号|URL>"
disable-model-invocation: true
allowed-tools: Bash(gh issue view:*), Bash(gh pr view:*), Bash(gh pr diff:*), Bash(gh label list:*), Bash(gh api:*), Bash(git log:*), Bash(git diff:*), Bash(git branch:*), Bash(date:*), Read, Glob, AskUserQuestion, Edit(tmp/**/issue-pr-sync_*.md)
---

# Issue / PR の同期更新

既存の Issue と PR の**根幹（背景・目的・解決方針）は正しい**前提で、そこから増えた差分を追従させる。想定する場面は、着手後に追加対応が入った・レビュー指摘に対応した結果、記述が実態とずれたとき。

書き換えではなく**追従**である。既存の構成と主張を壊さない。

| 対象 | 反映のしかた |
| --- | --- |
| Issue / PR の本文 | 更新案を提示し、承認を取ってから `gh` で直接反映する |
| サイドバー（ラベル・担当者・マイルストーン等） | 適用コマンドを提案するだけ。実行はユーザー |

サイドバーを直接変更しないのは、取り違えたときに誰が何を戻すべきか分かりにくく、プロジェクトごとの運用規約に依存するため。

## 参照ファイル

| ファイル | 役割 |
| --- | --- |
| `${CLAUDE_PLUGIN_ROOT}/references/markdown.md` | 本文を書くときの markdown 書式規約 |

## バリデーション

- `$ARGUMENTS` から Issue と PR の識別子を 2 つ取れない場合は、どちらが不足しているかを示して追加質問する
- 識別子は番号でも URL でもよい。`gh` は両方を受け付けるため、パースせずそのまま渡す
- `gh issue view` / `gh pr view` が失敗したら、その時点で中断して原因を報告する（存在しない・権限が無い・リポジトリが違う）

## 手順

### Step 1: 現状を集める

```bash
git branch --show-current
date +%Y%m%d_%H%M%S
gh issue view <Issue> --json number,title,body,labels,assignees,milestone,state,url
gh pr view <PR> --json number,title,body,labels,assignees,milestone,reviews,comments,baseRefName,state,isDraft,url
```

インラインのレビュースレッドは `gh pr view --json reviews` に本文が含まれないことがある。未解決の指摘を取りこぼしそうなときだけ `gh api repos/{owner}/{repo}/pulls/<PR>/comments` で補う。

### Step 2: 差分を読む

```bash
git log --oneline <baseRefName>..HEAD
git diff <baseRefName>...HEAD --stat
git diff <baseRefName>...HEAD
```

`baseRefName` は Step 1 の PR 情報から取る。ローカルに base ブランチが無い場合は `gh pr diff <PR>` に切り替える。

### Step 3: 更新案を作る

「本文の更新方針」に従って Issue 本文・PR 本文の更新後の全文を組み立てる。同時に「サイドバーの提案方針」でサイドバーの過不足を洗い出す。

更新が不要と判断した対象（本文・サイドバー）があれば、無理に変更を作らず「変更なし」とする。

### Step 4: 承認を取る

`AskUserQuestion` で、**本文を反映してよいか**を確認する。提示するのは次の 3 つ。

1. Issue 本文の変更点（どのセクションに何を足した / 直したかを箇条書きで。全文は出さない）
2. PR 本文の変更点（同上）
3. サイドバーの提案（適用コマンド。ここでは実行しない）

既存の記述と差分が食い違っていて、どちらを正とするか決めきれない論点があれば、承認を求める前にユーザーへ提示して判断を仰ぐ。

### Step 5: 本文を反映する

承認された対象だけを反映する。`--body` に長文を直接渡すとシェルの引用で壊れるため、必ずファイル経由で渡す。

```bash
gh issue edit <Issue> --body-file tmp/<ブランチ名>/issue-pr-sync_<ts>_issue.md
gh pr edit <PR> --body-file tmp/<ブランチ名>/issue-pr-sync_<ts>_pr.md
```

ブランチ名の `/` は置換せずサブディレクトリとして扱う（`commit` / `pr-create` と同じ規約）。反映前の本文は Step 1 で取得済みなので、戻す必要が出たときのために出力パスを報告に残す。

### Step 6: 報告する

- 反映した対象と、その URL
- 反映しなかった対象と、その理由
- サイドバーの適用コマンド（コピーして実行できる形で）
- 判断を保留した論点があればその内容

## 本文の更新方針

### 共通

- **既存の見出し構成を保つ。** テンプレート由来のセクションを削らない。空でも残す
- **背景・目的（Why）は原則そのまま残す。** 差分と明らかに矛盾する場合だけ、書き換えずにユーザーへ指摘する
- 既存本文に無い情報を推測で足さない。差分・コミット・レビューで裏が取れたものだけ書く
- 書式は `${CLAUDE_PLUGIN_ROOT}/references/markdown.md` に従う

### Issue 本文

- タスクリストがあれば、差分で**実装を確認できた項目だけ**チェックを入れる。確認できないものは外したままにする
- 着手後に増えた対応は、既存のタスクリストに項目として追記する。既存項目の文言は変えない
- 対応しないと決まった項目は消さず、そのまま残す（判断の経緯が読めなくなるため）

### PR 本文

- 「変更点」に、Step 2 のコミットと差分から**増えた分だけ**を追記する。既存の記述は、事実と食い違っていない限り書き換えない
- レビュー対応は「変更点」に、何を直したかとして 1 行で書く。レビューコメントの引用や返信は書かない（PR 上に既にある）
- `Closes #<番号>` / `Ref:` の記述は勝手に付け替えない。関連 Issue が増えた場合のみ、追加の可否をユーザーに確認する
- チェックリストは、差分から確認できた項目のみチェックする

## サイドバーの提案方針

`gh label list` で**リポジトリに実在するラベルだけ**を提案する。存在しないラベル名を提案しない。

| 項目 | 提案する条件 |
| --- | --- |
| ラベル | 差分の性質（バグ修正・機能追加・ドキュメント等）と現在のラベルが食い違うとき |
| 担当者 | 未設定のときだけ。設定済みなら触らない |
| レビュワー | 未設定で、かつリポジトリに CODEOWNERS があるときだけ言及する |
| マイルストーン | 未設定なら提案しない（運用に依存するため、ユーザーの判断に委ねる） |
| Projects | 同上 |

Development セクションの Issue 紐付けは `gh` の `edit` サブコマンドでは設定できない。必要なら PR 本文の `Closes #<番号>` で代替できる旨を添え、手動での紐付けを案内するに留める。

## 判断基準

- 根幹が間違っている（目的そのものが変わった）と判断したら、追従ではなく作り直しなので、その旨を報告して中断する
- 差分が空、またはレビュー指摘が無く既存本文と実態が一致している場合は「更新不要」と報告して終わる。形だけの更新を作らない
- PR が `merged` / `closed` の場合は本文を変更せず、理由を報告して終わる

## 禁止事項

- ❌ 承認を取る前に `gh issue edit` / `gh pr edit` を実行すること
- ❌ サイドバー（ラベル・担当者・マイルストーン・Projects）を直接変更すること
- ❌ 既存本文の見出し・セクションを削ること
- ❌ 差分から確認できない項目にチェックを入れること
- ❌ レビューコメントへ返信を投稿すること
- ❌ 本文を全面的に書き直すこと（このスキルは追従であり、作り直しではない）

$ARGUMENTS
